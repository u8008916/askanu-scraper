# DECISION_LOG.md

Record only decisions that change V3 contracts, architecture, source policy, scope, security/privacy rules, or schedule.

| Date | Decision | Why | Affected repos/docs | Owner | Approved by |
|---|---|---|---|---|---|
| 2026-09-06 | Freeze Courses/Programs schema v1: full 16-field normalized Scraper -> DB -> RAG record; year-scoped `entity_id`; namespaced stable `record_id`; explicit four-digit academic year; SHA-256 `content_hash` of canonical `content`; stored canonical URLs; strict no-invented-value/null policy; multi-year-safe RAG lookup; and first DB/migration ownership. | Unblock Will -> Carmen integration using one implementation-ready shared boundary based on the verified scraper output and Carmen's Day 2 exact-retrieval requirements. | RAG `docs/DATA_SCHEMA.md` and `docs/DECISION_LOG.md`; Scraper `docs/DATA_SCHEMA.md` and implementation require synchronisation before PR #5 merge. | Qasim (contracts/integration) | Qasim after Will/Carmen implementation review |
| 2026-09-11 | Day 7 requires durable `ingestion_runs` plus supplementary structured logs. Will owns record upsert/hash comparison/status detection and run writes. NEW/CHANGED set `index_status=PENDING` and clear `embedding_version`; UNCHANGED updates `last_seen_at`, preserves index state/version and emits no new signal. MISSING/failed collection preserves last-known-good. | Provide a stable audit path before Scheduler and make the changed-record handoff explicit across scraper and RAG. | Scraper persistence/tests/docs and RAG shared migration/read/index contract. | Qasim (contracts/integration) | Qasim |
| 2026-09-13 | Freeze Scholarships v1 identity as the canonical ANU detail slug with `record_id=scholarships:scholarship:<entity_id>`, and keep the approved Scholarship fields inside `metadata_json`. Scholarship filters are arrays with `[]` for no source evidence; scalar values remain nullable. Shared persistence must be generalized rather than adding a Scholarship-only table. | Keep Scraper, RAG and App aligned while allowing parser/local work to proceed without unsafe writes to the Courses-only shared table. | Scraper/RAG data schema, normalization, persistence and Scholarship query/resource contracts. | Qasim (contracts/integration) | Qasim |
| 2026-09-13 | Freeze the exact Scholarship detail URL boundary and lowercase alphanumeric/hyphen slug; exclude App Scholarship home/Finder navigation links from persisted-record identity; keep Scholarship application dates in metadata/content while top-level effective dates remain null; preserve `collected_at` for CHANGED/UNCHANGED; and persist RUNNING before the atomic accepted batch/final SUCCESS transaction with separate FAILED recovery. The scraper remains the trusted canonical-content SHA-256 writer. | Resolve the Day 9 Scraper/RAG/App persistence and link-provenance review without applying detail identity to navigation resources, broadening source provenance, misrepresenting application windows, changing existing timestamp semantics, or exposing partial successful batches. | Scraper/RAG/App validation, ingestion lifecycle, shared persistence tests and handoff documentation. | Qasim (contracts/integration) after Will review | Qasim |
| 2026-09-13 | Use `source_records` as the canonical writable shared table after the approved RAG migration; retain `course_program_records` only as a read-only Courses/Programs compatibility view. Keep Scholarship PostgreSQL execution disabled until PostgreSQL 18 migration, runtime grants, COMP1110 regression and Scholarship live-read gates pass. | Align the scraper writer with merged RAG PR #19 without risking writes through the compatibility view or enabling unverified Scholarship persistence. | Scraper PostgreSQL adapter/tests/deployment handoff and RAG migration contract. | Qasim (contracts/integration) after Carmen/Will review | Qasim |
| 2026-09-14 | Freeze Jobs v1 with `source_id=jobs_anu_search`, numeric requisition `entity_id`, `record_id=jobs:job:<entity_id>`, strict public ANU detail URLs, exact metadata keys including `employment_types` as an array and source-text `salary`, `current`/`closed`/null status, Canberra closing semantics, null top-level effective dates, numeric-ID lookup priority and deterministic current-job ordering. Continue using `source_records`; the next RAG migration follows live revision `20260914_0003`. Keep Jobs PostgreSQL writes and Scheduler activation gated behind controlled cloud evidence. | Unblock RAG Jobs implementation without independently invented identity, employment, salary, temporal, lookup or ordering semantics while preserving scraper provenance and rollout safety. | Scraper/RAG data schema, normalized fixtures, Jobs parser/model/repository/current endpoint and deployment handoffs. | Qasim (contracts/integration) after Carmen/Will review | Qasim |
| 2026-09-16 | Freeze Day 12 Accommodation v1 and Support v1 using the richer live-source-derived scraper shape. Accommodation uses `accommodation:residence:<slug>` and preserves source-backed fields without inferred vacancy; StarRez remains link-only. Support uses the invariant `support:support_service:<slug>`, retains approved internal Student Assistance topic URLs as topics, and permits source-published credential-free external HTTP(S) links only as unfetched referrals; ANUSA hosts are never referrals. Additional Support sources remain inactive. | Align scraper and RAG around one source-faithful record contract while preserving `record_id = domain:entity_type:entity_id`, strict source boundaries, and no-inference rules. | Scraper schema/model/parser/tests/handoff and Carmen's matching append-only RAG migration. PostgreSQL/runtime enablement remains gated on exact cross-repo validation. | Qasim (contracts/integration), Carmen (RAG), Will (scraper/data) | Qasim/Carmen |

If a decision changes a shared contract, update every affected repo in the same work cycle.

## Documentation-only bootstrap record — 2026-09-05

This separate record captures the requested bootstrap clarification and outstanding gaps, not a change to locked V3 decisions. The three-repo architecture, roles, dates/schedule, six domains, sources, GCP region assumption and source policy remain unchanged.

- Scope: documentation/bootstrap scaffold only. Will's Day 1 implementation is outstanding; this cleanup adds no Python package, dependencies, source modules, registry enforcement, machine-readable registry, HTML fixtures or parser tests, and performs no live requests or mass crawling.
- Repository hygiene: remove README trailing whitespace and align credential/private-key ignore patterns with the RAG repo while keeping `.env.example` trackable.
- Source gaps: Will's machine-readable registry needs stable `source_id`, parser mapping and approval/active state. A bounded fetch/rate policy and exact additional Support targets remain unresolved. Document an exact production Events target if different from the approved official ANU Events/calendar fallback. Qasim coordinates source approvals; no values or targets are invented here.
- Contract gaps: precise field types/nullability, status values and ID/hash rules remain for the Qasim/Carmen/Will schema coordination before implementation. Authoritative API/conversation contracts remain in `askanu-rag/docs/API_CONTRACT.md` and `askanu-rag/docs/CONVERSATION_CONTRACT.md`; no API contract is copied here.
- Preserved safeguards: Rubric is `PENDING_APPROVAL`, non-production pending documented approved access; no undocumented/internal API integration. Official ANU Events/calendar is the release-safe fallback/primary source unless Rubric approval is obtained and remains the release source regardless of the response. No authenticated StarRez scraping; no collector targets outside the eventual approved registry; failed or suspicious collection preserves last-known-good data.

Affected documentation: `README.md`, `.gitignore`, `docs/SOURCE_REGISTRY.md`, `docs/DATA_SCHEMA.md` and this log. These entries record the user-authorized documentation cleanup, not approval of unresolved implementation choices.
